{
  "name": "workwarden",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test": "vitest run",
    "routes": "node scripts/route-surface.mjs",
    "routes:check": "node scripts/route-surface.mjs --check",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.13.7",
    "jose": "^6.2.12",
    "zod": "^4.6.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^22.20.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.130.0"
  }
}
